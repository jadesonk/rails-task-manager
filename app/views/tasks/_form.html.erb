<%= simple_form_for(task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.input_field :completed, as: :boolean if @task.persisted? %> <%= @task.persisted? ? 'Completed' : '' %>
  <%= f.submit class: 'btn btn-primary btn-block' %>
<% end %>
